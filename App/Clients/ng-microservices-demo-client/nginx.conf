events{}

http {

    include /etc/nginx/mime.types;

    server {
        listen       8080;
        server_name  localhost;

        location / {
            root   /etc/nginx/html/ng-microservices-demo-client;
            try_files $uri $uri/ /index.html =404;
        }
    }
}
